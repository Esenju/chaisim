// Network topology
topology = mesh;
k = 2;  // 2x2 mesh (4 nodes)
size = 2;

// Routing
routing = xy;
vc_buf_size = 8;
vcs = 4;

// Link properties
link_latency = 2;  // Cycles
link_bandwidth = 256;  // Gb/s

// Traffic (configured via Gem5 bridge)
traffic = custom;